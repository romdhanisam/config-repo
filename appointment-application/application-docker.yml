
prop1: "value1"
prop2: "value2"
user:
  role: Developer from docker profile

management:
  endpoints:
    web:
      exposure:
        include: "*"
spring:
  cloud:
    consul:
      discovery:
        prefer-ip-address: true
        register-health-check: true
      host: consul
      port: 8500
      config:
        enabled: false